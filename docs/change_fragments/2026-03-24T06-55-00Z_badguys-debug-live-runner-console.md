# BadGuys debug live runner console

- clarify that debug console mode mirrors live AM Patch Runner IPC log messages to stdout during RUN_RUNNER steps
- no change to quiet, normal, verbose, heartbeat, summary, or subprocess stdio artifact semantics
